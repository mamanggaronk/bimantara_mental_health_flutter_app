import 'package:flutter/material.dart';

class QuoteText extends StatelessWidget {
  const QuoteText({super.key});

  @override
  Widget build(BuildContext context) {
    // * TODO : Implement QuoteText
    return Container(
      child: Text(''),
    );
  }
}